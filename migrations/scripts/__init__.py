"""ImperialReminder migration scripts.

Each script is standalone and re-runnable:

    python -m migrations.scripts.<name>            # dry run (default, no writes)
    python -m migrations.scripts.<name> --apply    # perform the writes
"""
