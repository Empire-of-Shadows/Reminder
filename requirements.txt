# --- Core bot ---
discord.py~=2.7
loguru~=0.7.2            # engine storage/log logging backend
python-dotenv~=1.2.2     # 1.1.x has CVE-2026-28684
pymongo~=4.17.0          # async client (AsyncMongoClient) + bson
dnspython~=2.8.0         # mongodb+srv URI resolution
aiohttp~=3.14.1          # 3.12.x/3.13.x have multiple CVEs (zip-bomb / DoS / SSRF); fixed in 3.14.0+
pytz~=2026.2
backoff~=2.2.1
tabulate~=0.10.0

# --- Dashboard (FastAPI web UI + shared-session OAuth) ---
fastapi~=0.136.3         # 0.115.x pulls starlette <0.47 (CVE-2025-54121 / -62727)
uvicorn~=0.49.0
httpx~=0.28.1
itsdangerous~=2.2.0
