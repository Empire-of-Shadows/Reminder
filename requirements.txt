# --- Core bot ---
discord.py~=2.7
python-dotenv~=1.2.2     # 1.1.x has CVE-2026-28684
pymongo~=4.13.2          # async client (AsyncMongoClient) + bson
aiohttp~=3.13.5          # 3.12.x has multiple CVEs (zip-bomb / DoS / SSRF)
pytz~=2025.2
backoff~=2.2.1
tabulate~=0.10.0

# --- Dashboard (FastAPI web UI + shared-session OAuth) ---
fastapi~=0.115.0
uvicorn~=0.35.0
httpx~=0.27.0
itsdangerous~=2.2.0