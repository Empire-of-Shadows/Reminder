"""Public /help command for ImperialReminder.

``help_commands.py`` holds the cog and the Components v2 panel view; ``help_content.py``
is a plain-data module with the category text (no ``setup()``, so the cog loader skips it).
"""
